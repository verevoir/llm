# Examples

Runnable usage examples per subpath. Each example targets the published surface,
not the internal types.

Planned set:

- `anthropic-chat.ts` — minimal `chat()` call against Anthropic.
- `anthropic-tools.ts` — `chatWithTools()` + streaming `report_progress`.
- `cost-rollup.ts` — `sumUsages` + `formatTokensCompact` across a session.

These examples land with the extraction slice. Until then, this directory holds
the planned structure only.
