# Probe findings

Minimal test content.

## What each probe establishes

1. **`run-probe.sh`** (`--safe-mode` on, 3 turns). Confirmed something.

2. **`run-probe-no-safe-mode.sh`** (`--safe-mode` dropped). Confirmed something else.
